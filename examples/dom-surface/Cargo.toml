[package]
name = "dom-surface-example"
version = "0.1.0"
edition = "2024"

[target.'cfg(target_os = "macos")'.dependencies]
env_logger = "0.11.8"
nestix = { git = "https://github.com/wirelesseye/nestix.git" }
nestix-native = { path = "../../nestix-native", default-features = false, features = ["appkit", "dom"] }
